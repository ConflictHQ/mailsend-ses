{
  "name": "mailsend",
  "version": "0.1.0",
  "private": true,
  "description": "SES deliverability test tool on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "wrangler": "^4.78.0"
  }
}
